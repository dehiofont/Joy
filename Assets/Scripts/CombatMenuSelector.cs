using UnityEngine;
using UnityEngine.UI;

public class CombatMenuSelector : MonoBehaviour
{
    private Image armamentSelector;
    private Image targetSelector;
    private Image partSelector;

    private void Start()
    {
        //armamentSelector = GameManager.Instance.armamentSelector;
        //targetSelector = GameManager.Instance.targetSelector;
        //partSelector = GameManager.Instance.partSelector;
    }


}
